// Цикл While
let start_number = 33;
while (start_number >= 23) {
  if (start_number % 3 === 0) {
    console.log(start_number);
  }
  start_number--;
}

// Цикл Do/While
// let i = 0;
//   const numMin = 1;
//   const numMax = 10;
// do {
    // let randomNumber = Math.floor(Math.random() * (numMax - numMin + 1)) + numMin;
  
//   i++;
//   console.log(randomNumber);
// } while (randomNumber !== 8);
// console.log('Завершення циклу Do/While ');
// console.log(`Кількість виконаних ітерацій склала: ${i}`);

// Цикл For
// for (let i = 2; i <=10 ++i) {
//   console.log(`Число 2 в степені ${i} дорівнює ${Math.pow(2, i)}`);
// }