// let myNumber = 10;
let myNumber = -7;

let message; 

if (myNumber % 2 === 0) {
    message = `Число "${myNumber}" парне`;
} else message = `Число "${myNumber}" непарне`;

console.log(message);

if (condition) {
    
} else {
    
}


if (myNumber % 2 === 0) message = `Число "${myNumber}" парне`;
